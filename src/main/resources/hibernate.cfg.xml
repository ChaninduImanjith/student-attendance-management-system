<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/sams_db?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property> <!-- Please adjust this according to your local setup -->

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Update the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>
        
        <!-- Entity Mappings -->
        <mapping class="com.sams.entity.User"/>
        <mapping class="com.sams.entity.Course"/>
        <mapping class="com.sams.entity.Subject"/>
        <mapping class="com.sams.entity.Student"/>
        <mapping class="com.sams.entity.Lecturer"/>
        <mapping class="com.sams.entity.ClassSchedule"/>
        <mapping class="com.sams.entity.Attendance"/>
    </session-factory>
</hibernate-configuration>
